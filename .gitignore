# Build artifacts
/build/

# Distlib companion (leaf-requirements manifest emitted alongside
# dist/hisab.cyr by `cyrius distlib`; only the bundle itself is tracked)
/dist/hisab.deps

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Toolchain artifacts (downloaded by CI / install scripts)
cyrius-*.tar.gz

# Environment / secrets
.env
.env.*

# Rust era (archived in pre-2.0 git tags; ignore if present locally)
/rust-old/target/
/target/
criterion/
proptest-regressions/
supply-chain/.cache/
lcov.info
tarpaulin-report.html
coverage/
*.pdb
