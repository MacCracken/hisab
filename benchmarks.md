# Benchmarks

Latest: **2026-05-28T23:45:25Z** — commit `e1f8f4c`

## vec3_add:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `vec3_add:` | 189000.0 ns |

## vec3_cross:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `vec3_cross:` | 384000.0 ns |

## vec3_normalize:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `vec3_normalize:` | 95000.0 ns |

## quat_mul:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `quat_mul:` | 90000.0 ns |

## quat_slerp:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `quat_slerp:` | 108000.0 ns |

## quat_rotate_vec3:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `quat_rotate_vec3:` | 86000.0 ns |

## m4_mul:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `m4_mul:` | 56000.0 ns |

## m4_inverse:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `m4_inverse:` | 62000.0 ns |

## m4_transform_point:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `m4_transform_point:` | 53000.0 ns |

## t3d_compose:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `t3d_compose:` | 55000.0 ns |

## ray_sphere:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `ray_sphere:` | 143000.0 ns |

## ray_aabb:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `ray_aabb:` | 27000.0 ns |

## ray_triangle:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `ray_triangle:` | 61000.0 ns |

## srgb_to_linear:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `srgb_to_linear:` | 23000.0 ns |

## tonemap_reinhard:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `tonemap_reinhard:` | 50000.0 ns |

## calc_derivative:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `calc_derivative:` | 32000.0 ns |

## calc_integral_simpson:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `calc_integral_simpson:` | 25000.0 ns |

## num_gcd:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `num_gcd:` | 24000.0 ns |

## num_is_prime:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `num_is_prime:` | 44000.0 ns |

## cx_mul:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `cx_mul:` | 62000.0 ns |

## ease_in_out:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `ease_in_out:` | 21000.0 ns |

## perlin_2d:

| Benchmark | Current (`e1f8f4c`) |
|-----------|------|
| `perlin_2d:` | 18000.0 ns |

---

Generated by `./scripts/bench-history.sh`. History in `bench-history.csv`.
