# Audit — 2026-08-04 (v2.8.1), full sweep of the v2.8.0 tree

Six dimensions, each swept by an independent agent and then **adversarially verified** by a second
agent that reproduced every finding with its own harness before it was allowed into this report.

**42 confirmed, 0 refuted** — 4 critical, 17 high, 16 medium, 5 low.

The zero-refutation rate is worth reading carefully rather than as a compliment to the finders. The
previous two audits refuted ~15%. What changed is that the verifiers were required to *reproduce by
execution*, so speculative findings did not survive to be counted — several were dropped inside the
verify step rather than arriving here as refutations. Two findings were **downgraded** on
verification and one was materially corrected; those notes are kept inline.

## The headline: a defect in what 2.8.0 shipped

`delaunay_2d` was rewritten in 2.8.0 with triangle adjacency, verified across 298 fixtures, and the
result was correct on every one. It is still **incomplete on ordinary uniform-random input** —
roughly 15–37% of point sets come back missing at least one triangle, silently, with no error
signal. The cause is not the rewrite: `d_max = max(dx, dy) * 10` builds a super-triangle only 10x
the bounding box, and Bowyer-Watson only reduces to the true Delaunay triangulation once no
super-vertex lies inside any real circumcircle. At 10x that condition is violated, the real triangle
is never created, and the compaction pass drops the super-vertex triangle that covered the gap.

Proven by exact rational arithmetic: over 40 point sets, the set of triangles whose circumcircle
encloses a super-vertex matched the observed missing set **40/40**. At multiplier 1e3 and above the
failures vanish for uniform data.

**But the fix is not simply a bigger multiplier.** The verifier ran the same substitution on tight
clusters and found 1e9 *breaks* input the current 10x handles correctly (span 1e-8: 20 missing and 9
spurious overlapping triangles). The 10 is a real trade-off between super-triangle adequacy and the
non-adaptive in-circle determinant's conditioning — the two defects are coupled, exactly as
`issues/2026-08-04-incircle-precision.md` warned. **1e6 is a defensible interim value; 1e9 must not
be used.** The durable fix is an adaptive predicate or symbolic ghost-vertex handling.

None of the 1801 assertions detects any of this. Every existing `delaunay_2d` assertion checks
either a fixture where the shipped code is right, or a structural property that under-coverage does
not violate.

## Findings

| Sev | Dimension | Finding | Site |
|---|---|---|---|
| **critical** | numerical | mpr_penetration returns a wrong (frequently zero or negative) penetration depth for any non-axis-aligned overlap — 65 of 81 sampled box/box configurat | `/home/macro/Repos/hisab/src/collision_core.cyr:151-224 (portal built through the` |
| **critical** | performance | gjk_epa_3d is O(k²) in EPA iterations — 8,576 face evaluations, 1.24 MB of never-freed arena and milliseconds per single narrowphase call | `src/geo_advanced.cyr:394 (closest-face rescan) and src/geo_advanced.cyr:430 (vis` |
| **critical** | performance | gjk_epa_3d and mpr_penetration return wrong penetration depths for sphere-vs-box | `src/geo_advanced.cyr:308 (gjk_epa_3d), src/collision_core.cyr:150 (mpr_penetrati` |
| **critical** | regression | delaunay_2d's 10x super-triangle leaves holes in the mesh on ordinary uniform-random input (silent incomplete triangulation) | `/home/macro/Repos/hisab/src/collision_mesh.cyr:181 (`var d_max = f64_mul(f64_max` |
| **high** | api-consistency | time_of_impact returns 1 ("collision found") while storing a NULL HVec3 pointer into out_normal — it ignores gjk_epa_3d's failure return ⚠️ | `src/geo_advanced.cyr:773 and :820 (both stores verified present at those exact l` |
| **high** | api-consistency | bvh_query_ray silently drops entire subtrees: geo_ray_aabb returns 0 for both "miss" and "hit at t=0", and the BVH walker uses `== 0` as its prune tes | `src/geo_advanced.cyr:641 (verified: `if (geo_ray_aabb(ray, aabb) == 0) { return ` |
| **high** | memory-safety | svd_golub_kahan writes past out_U whenever rows < cols — its own "m >= n" contract is never checked | `src/linalg_precision.cyr:752 (entry, `fn svd_golub_kahan`); OOB store at src/lin` |
| **high** | memory-safety | tensor_add / tensor_scale / tensor_contract dereference einsum's and tensor_new's documented 0-on-failure return, and tensor_add over-reads b on shape | `src/tensor.cyr:88 (tensor_add), src/tensor.cyr:103 (tensor_scale), src/tensor.cy` |
| **high** | memory-safety | calc_partial_derivative bounds-checks only the upper end of `index`; a negative index reads and writes before the buffers | `src/calc_ext.cyr:60` |
| **high** | memory-safety | tensor_contract never validates idx_i / idx_j against the tensor's rank ⚠️ | `src/tensor.cyr:121` |
| **high** | numerical | sequential_impulse never converges for restitution >= 1 (period-2 oscillation, answer depends on the parity of `iterations`); restitution has no effec | `/home/macro/Repos/hisab/src/collision_core.cyr:294 (delta) and :306-316 (frictio` |
| **high** | numerical | time_of_impact misses real collisions — 48 of 101 swept sphere pairs report "no collision" although contact occurs well inside max_t; the doc claims c | `/home/macro/Repos/hisab/src/geo_advanced.cyr:748 (doc: "Uses conservative advanc` |
| **high** | numerical | calc_monotone_cubic is not monotone — the Fritsch-Carlson sign condition is missing, so the interpolant overshoots both endpoints on a strictly monoto | `/home/macro/Repos/hisab/src/calc_ext.cyr:667-687 (monotonicity constraint block)` |
| **high** | performance | spatial_hash_* has a hard-coded 1,024 buckets that never grows, so every query costs Θ(n/1024) instead of Θ(1) | `src/spatial.cyr:810 (`var _SH_BUCKET_COUNT = 1024;`), consumed at :821 (mask), :` |
| **high** | performance | _bvh_build_rec recomputes every primitive's centroid at every tree level and allocates 5 heap blocks per bounds merge — 36.6 MB of arena to build a 16 | `src/geo_advanced.cyr:555 (`bounds = _bvh_aabb_merge(...)`) and src/geo_advanced.` |
| **high** | regression | cmat_kronecker, cmat_mul_vec and cmat_exp dereference the capped-constructor 0 that the other seven cmat_* functions guard | `/home/macro/Repos/hisab/src/complex.cyr:379 (cmat_kronecker `var ar = cmat_rows(` |
| **high** | test-quality | Every ODE test integrand reaching ode_dopri45 is autonomous, so the DOPRI45 abscissa row (c2..c7) is certified by nothing ⚠️ | `src/ode.cyr:157,162,169,177,186 (stage times t2..t6); fixtures tests/hisab.tcyr:` |
| **high** | test-quality | All four noise functions can be replaced by `return 0;` and the suite stays green ⚠️ | `src/noise_simplex.cyr:96 simplex_2d, :177 simplex_3d; src/calc.cyr:446 perlin_2d` |
| **high** | test-quality | christoffel_symbols is only ever called with an all-zero metric-derivative array, so its formula is unreachable by the suite | `src/diffgeo.cyr:64-104 (bracket at :90); fixtures tests/modules.tcyr:1325-1332 a` |
| **high** | test-quality | opt_bfgs, opt_lbfgs and opt_levenberg_marquardt are asserted only through their input-validation guards; ~490 lines of solver body never run | `src/optimize.cyr:282 opt_bfgs, :434 opt_lbfgs, :631 opt_levenberg_marquardt (fil` |
| **high** | test-quality | num_tridiag_solve is tested only on a DIAGONAL system, so the entire Thomas elimination and back-substitution are multiplied by zero | `src/num_ext.cyr:704-773 (back-substitution at :769); fixture tests/modules.tcyr:` |
| **medium** | api-consistency | gjk_epa_3d returns 0 ("not penetrating") for shapes its own sibling gjk_intersect_3d reports as overlapping, and leaves both out-params unwritten ⚠️ | `src/geo_advanced.cyr:341 and :348 (verified at those exact lines)` |
| **medium** | api-consistency | halfedge_from_triangles aborts the process instead of returning the documented 0 on error ⚠️ | `src/collision_mesh.cyr:472 (fn declaration verified at that line; doc block at :` |
| **medium** | api-consistency | ivl_div's divide-by-zero widening returns a finite [-1e9, 1e9] that does not enclose the true range — the module's own stated soundness contract | `src/interval.cyr:86-90 (verified: the f64_le/f64_ge zero-straddle guard returnin` |
| **medium** | api-consistency | einsum accepts a repeated output label and returns a confidently wrong tensor instead of the documented 0-on-error | `src/einsum.cyr:169 (verified: `var out_rank = _einsum_parse_operand(ndata, outpu` |
| **medium** | memory-safety | cmat_exp / cmat_mul_vec / cmat_kronecker / cmat_inverse dereference the designed 0 that cmat_new and cmat_mul return — the 2.7.0-B null-guard family i ⚠️ | `src/complex.cyr:378 (cmat_kronecker), src/complex.cyr:412 (cmat_exp), src/comple` |
| **medium** | memory-safety | num_halton crashes with SIGFPE on base 0 and never terminates on base 1 | `src/num_ext.cyr:645-646 (fn at 639)` |
| **medium** | memory-safety | spatial_hash_query_radius has an unbounded (radius/cell_size)^3 triple loop | `src/spatial.cyr:909-952 (fn at 909; cells_r computed at 916, dx/dy/dz loops 918-` |
| **medium** | numerical | svd_golub_kahan silently returns wrong singular values for a wide matrix (m < n) — the documented m >= n precondition is never checked and the functio | `/home/macro/Repos/hisab/src/linalg_precision.cyr:752 (no m>=n guard at entry; co` |
| **medium** | performance | num_dct / num_idct / num_dst / num_idst are O(n²) while an O(n log n) num_fft ships alongside them | `src/num_ext.cyr:454 (num_dct), :490 (num_idct), :394 (num_dst), :432 (num_idst)` |
| **medium** | test-quality | calc_hessian's mixed-partial block is never asserted, and the test function is separable so the true value is zero anyway ⚠️ | `src/calc_ext.cyr:194-233 (4-point cross stencil at :225); fixture tests/modules.` |
| **medium** | test-quality | calc_catmull_rom is tested only at t=0 and t=1 on collinear equally-spaced points, where the t^2 and t^3 coefficients are identically zero ⚠️ | `src/calc.cyr:219-257 (`_cmr_comp`, coeff2 at :249, term2 at :251, term3 at :255)` |
| **medium** | test-quality | Adaptive Simpson is tested only on x^2, for which a single unrefined Simpson panel is already exact, so the entire refinement machinery is dead | `src/calc_ext.cyr:244-317 (`_adaptive_simpson_rec` / `calc_adaptive_simpson`); so` |
| **medium** | test-quality | ode_bdf is exercised only at order 4 with f == 0 and a constant history, leaving beta unconstrained and orders 2/3/5 unreachable | `src/ode.cyr:415-480 (order-2 branch :431, order-3 :437, order-5 :456, `beta = _B` |
| **medium** | test-quality | The se3_adjoint assertion passes a 24-byte HVec3 where a 48-byte twist is required; the non-null check hides a 24-byte out-of-bounds read | `tests/modules.tcyr:3359 `assert_neq(se3_adjoint(se_g, hvec3_unit_x()), 0, ...)`;` |
| **medium** | test-quality | so3_from_mat3 documents a validation it does not perform, and the only assertion is that it returns non-null | `src/lie_ext.cyr:134-138 (comment 'Validate and return a Mat3 as SO(3) (Checks de` |
| **medium** | test-quality | delaunay_2d normalises every emitted triangle to CCW and nothing verifies the winding | `src/collision_mesh.cyr:419-431 (orientation test at :424); assertions tests/modu` |
| **low** | api-consistency | eigen_symmetric and eigen_qr carry the same documented contract but return eigenvalues in different orders, and eigen_symmetric's doc names a max_iter ⚠️ | `src/linalg_ext.cyr:852 (doc `Returns:` line) / :853 (signature). Note the findin` |
| **low** | api-consistency | geo_ray_plane's doc block states the opposite convention from the code, in the line immediately above the note that corrects it | `src/geo.cyr:220` |
| **low** | api-consistency | spatial_hash_insert returns 0 on success while its sibling inserts return 1 on success | `src/spatial.cyr:859` |
| **low** | memory-safety | quadtree_insert / octree_insert recurse to max_depth on coincident points, with no cap on max_depth | `src/spatial.cyr:487 (_qt_insert_rec) / src/spatial.cyr:702 (_ot_insert_rec); con` |
| **low** | performance | Four residual allocation-inside-loop sites in a never-freeing bump allocator | `src/spatial.cyr:210, src/tensor.cyr:149, src/linalg_precision.cyr:1300, src/geo_` |

⚠️ = severity changed on verification.

Full mechanism, evidence and proposed fix for every finding are in the workflow record; the
per-dimension `clean` notes below record what was checked and found **sound**, so the next audit
does not re-plough it.

## What is clean

### api-consistency

Checked and found SOUND — do not re-plough:

FLAT-NAMESPACE HAZARD IS CLOSED. Scripted scan of all 34 src/*.cyr for duplicate `fn`, top-level `var`, `struct` and enum-member definitions: ZERO duplicates. Scripted scan of every src/ symbol against every lib/ (stdlib + sakshi) symbol: ZERO shadows. So last-definition-wins cannot currently reorder behaviour between `cyrius.cyml [lib]` order and tests/*.tcyr include order. (Script kept at /tmp/claude-1000/-home-macro-Repos-hisab/02f8a39a-a230-4034-83fe-7d1f605e314f/scratchpad/scan.py.)

DOC-CLAIMED ERROR CODES ALL REACHABLE. Scripted every doc block against its own function body for HSB_ERR_* codes named in the doc but never returned. Only 3 hits, all false positives: _lp_split_zero_diag, svd_golub_kahan and eigen_qr propagate HSB_ERR_NO_CONVERGENCE out of helpers via `err`, so the literal is not in their own body. No function documents an error code it cannot produce.

RAY / OVERLAP SIBLINGS AGREE. mpr_intersect vs gjk_intersect_3d (both documented "1 if shapes overlap, 0 otherwise") agree on all 41 sphere-vs-sphere separations d = 0.0 … 4.0 step 0.1, and on all 41 box-vs-box separations over the same range. No disagreement at any distance including exact tangency.

NUMBER THEORY / TRANSFORMS. num_halton(i,2) and num_sobol(i) stay inside the documented [0,1) for i = 1..199 (0 violations). num_modinv's -1 sentinel is unreachable as a legitimate result (the result is normalised into [0,m) before return), so the sentinel is detectable. num_crt correctly returns HSB_ERR_INVALID_INPUT (-10) for non-coprime moduli (4,6) and leaves out_x/out_m untouched. num_tridiag_solve returns HSB_ERR_DIVISION_BY_ZERO (-4) on a zero leading pivot as documented. num_is_prime(0)=0, (1)=0, (-7)=0 — the doc's "1 if prime, 0 if composite" is imprec

### memory-safety

Verified sound by reading and/or execution — do not re-plough:

VEC-BACKED PATHS ARE FAIL-SAFE. lib/vec.cyr:78-92 bounds-checks vec_get/vec_set in both directions and calls _vec_die() (syscall(60,1)), so every vec-indexed algorithm aborts with exit 1 rather than corrupting memory. I confirmed this empirically: kdtree_build(pts, -1) prints "vec: index out of bounds" and exits 1, not 139. That covers delaunay_2d, convex_hull_2d, triangulate_polygon, halfedge_from_triangles/adjacent_faces/is_boundary, bvh_build, sequential_impulse, gjk_epa_3d and the quadtree/octree point stores. Do not spend time hunting OOB through vec — only raw alloc/load64/store64 sites are exposed.

diffgeo.cyr: I read all 14 public entry points. Every one carries BOTH `if (dim <= 0 || dim > _DG_MAX_DIM) { return 0; }` and explicit null guards on its tensor arguments (ricci_tensor:189-190, ricci_scalar:224-225, einstein_tensor:246-247, weyl_tensor, sectional_curvature, geodesic_*, parallel_transport, killing_residual). The 2.7.0-C repair is complete here. Only the raw `*_get` accessors (christoffel_get:108, riemann_get:176, ricci_get:215, weyl_get:409) are unchecked, which is the conventional contract for a flat-index accessor.

optimize.cyr: opt_gradient_descent, opt_conjugate_gradient, opt_bfgs, opt_lbfgs and opt_levenberg_marquardt all reject n <= 0 and all check every alloc/mat_new return against 0 before use (lines 138, 197, 461-464, 656-659). opt_bfgs and opt_lbfgs additionally enforce _OPT_MAX_DIM = 4096. The 2.6.14/2.6.15 loop-allocation hoists (L-BFGS g_new_buf/s_k/y_k, LM jtj/lu/rhs/piv, BiCGSTAB `s`) are all present, so there is no per-iteration bump leak left in these five.

_bvh_build_rec (geo_advanced.cyr:544-621): the degenerate-split clamp at 614-616 provably leaves split strictly ins

### numerical

Verified sound by running against exact/independent references (do not re-plough):

DIFFGEO — the whole christoffel_symbols -> riemann_tensor -> ricci_tensor -> ricci_scalar -> einstein_tensor -> sectional_curvature chain, built from the unit 2-sphere metric g = diag(1, sin^2 th) at th = pi/3 (a case the suite never builds; its diffgeo tests use hand-written space-form Riemann arrays or the all-zero flat metric). Every value exact: Gamma^th_phiphi = -0.4330127018922193, Gamma^phi_thphi = 0.5773502691896257, R^th_phithphi = sin^2 th = 0.75, R_thth = 1, R_phiphi = 0.75, Ricci scalar = 2.0, Einstein tensor = 0 both diagonal entries, K(e0,e1) = 1.0. Index conventions and the sectional-curvature sign convention are mutually consistent.

SPLINES — calc_bspline exact against (a) the closed-form cubic Bezier for the clamped knot vector [0,0,0,0,1,1,1,1] and (b) an independent Python de Boor for the non-uniform clamped vector [0,0,0,0,1,2,3,3,3,3] with 6 control points; agreement to the last digit at 5 and 7 sample points. calc_hermite_tcb with T=C=B=0 equals calc_catmull_rom bit-for-bit on uniform knots (cross-module convention check). cubic_bezier_ease matches a 200-step bisection reference for CSS ease, ease-in and ease-in-out to <= 1e-12 at 11 sample points each.

TRANSFORMS — num_dct/num_idct and num_dst/num_idst match their own documented kernels computed in numpy and round-trip to ~5e-16; num_fft_2d matches numpy.fft.fft2 on a 2x4 ramp and num_ifft_2d round-trips exactly. num_halton (bases 2 and 3) and num_sobol match the van-der-Corput / Antonov-Saleev references (sobol = 0, .5, .75, .25, .375, .875, .625, .125 — note the doc comment calls it "Van der Corput" but the Gray-code output is a valid Sobol, only the wording is loose).

NUMBER THEORY — num_euler_totient(1..40),

### performance

Checked and found sound — do not re-plough these. All timings from purpose-built scratch harnesses under /tmp/claude-1000/-home-macro-Repos-hisab/02f8a39a-a230-4034-83fe-7d1f605e314f/scratchpad/ (geo_scale.cyr, misc.cyr, last.cyr, sym.cyr), reading lib/alloc.cyr's `_heap_used` global for exact allocated-bytes deltas. Repo left untouched (git status clean).

RIGHT EXPONENT, verified by growth ratio per doubling:
- delaunay_2d (the 2.8.0 adjacency rewrite): 2954/6468/15357/34538/86088 us at n=500..8000 → 2.19/2.37/2.25/2.49x, i.e. ~n^1.25. The walk-plus-flood-fill really did kill the old O(n²) Bowyer-Watson; 15,945 triangles at n=8000, flat 1,360 B/point.
- convex_hull_2d: 2077/4435/9545/20480/44573 us at n=2000..32000 → 2.13/2.15/2.15/2.18x, O(n log n), 16 B/point.
- kdtree_build: 1328/2824/6011/12887 us at n=2000..16000 → 2.13/2.13/2.14x. kdtree_nearest × n: 818/1763/3770/8446 us → 2.15/2.14/2.24x. The 2.7.0 midpoint-with-median-fallback partition holds on uniform input; 40 B/query.
- halfedge_from_triangles: 1208/2550/4817/9962 us at 2000..16000 triangles → linear; the 2.6.15 hashed twin pairing is intact.
- detect_islands: 173/317/625 us at n=2000..8000 → linear. _col_uf_find has path halving and _col_uf_union has union-by-rank.
- quadtree_insert: 826/1707/3606 us → 2.07/2.11x. quadtree_query is FLAT at 332/338/363 us for 500 queries as n goes 2000→8000, i.e. the tree genuinely prunes (contrast with the spatial hash finding).
- bvh_query_ray: sublinear, 5403/7611/12014/13926 us for 500 rays as n goes 2000→16000; 76,000 B per 500 queries independent of n. Only bvh_BUILD is a problem, not the traversal.
- num_fft: 223/503/1064/2297 us at n=1024..8192 → 2.26/2.11/2.16x, zero heap. It is the correct reference the DCT family should be using.
- svd_golub_kahan: 579/4484/342

### regression

Everything below was verified BY EXECUTION against an independent reference or a brute-force oracle, not by reading the CHANGELOG. All reproducers are under /tmp/claude-1000/-home-macro-Repos-hisab/02f8a39a-a230-4034-83fe-7d1f605e314f/scratchpad/. Repo tree untouched (git status clean). Baseline re-confirmed: hisab 310, foundation 349, modules 927, edge_cases 215 = 1801/1801 pass.

k-d TREE SPLIT PLANE (src/spatial.cyr:144 _kd_partition, :211 _kd_build_rec) -- HOLDS. scratchpad/kd.tcyr, 8/8 pass. 1500 uniform points, 400 radius-60 queries, counts compared against exact brute force: 0 mismatches (the pre-fix defect was 274/400 wrong). kdtree_nearest matched brute force on all 400. Deliberately drove the QUICKSELECT FALLBACK with 600 geometrically-spaced coordinates (0.75^i ladder, the distribution the midpoint split degenerates on): 200 radius queries + 200 nearest queries, 0 mismatches. 2000-point set with 1990 exactly coincident points: builds without recursion blow-up and the radius query returns exactly 1990.

svd_golub_kahan HOUSEHOLDER BALANCING + U TRANSPOSE (src/linalg_precision.cyr:752, :764 _lp_pow2_floor) -- HOLDS. scratchpad/svd.tcyr, 9/9 pass. 240 random full-rank m x n (n=2..5, m=n..n+2): all converge, max |A - U*S*Vt| / max|A| under 1e-8 on every one, and U^T U == I plus Vt Vt^T == I to 1e-8 on every one (a transposed U would fail reconstruction while passing orthonormality, so both are checked). 180 rank-deficient matrices (column n-1 forced to 2 x column 0): 0 non-convergences, 0 reconstruction failures. Balancing exercised at 7 magnitudes x 20 matrices (1e-250, 1e-100, 1e-14, 1, 1e16, 1e100, 1e250): all converge, relative reconstruction holds at every scale.

eigen_qr (src/linalg_precision.cyr:1179 balance) -- HOLDS. 200 random symmetric n x n, n=2..6: 

### test-quality

Method: I copied the tree into the scratchpad (repo left read-only; `git status --porcelain` is empty and the baseline still reads 310/349/927/215 = 1801/1801) and ran a mechanical operator-swap mutation campaign (f64_add<->f64_sub, f64_mul->f64_add, f64_div->f64_mul, f64_lt->f64_gt, F64_HALF->F64_ONE, f64_neg->f64_abs) plus targeted semantic mutations, scoring each mutant against all four suites. Campaign kill rates measured: linalg_precision 90/120 killed, collision_mesh 59/70, num_ext 60/70, spatial 14/36, ode 62/110, calc_ext 92/120, optimize 14/76.

Verified SOUND, do not re-plough:

- `_lp_split_zero_diag` (src/linalg_precision.cyr:423), the rank-deficient SVD split path added in 2.6.13, IS covered: stubbing it to `return 0;` produces 7 failures in tests/hisab.tcyr. The SVD/eigen tests are genuinely load-bearing.
- The 30 linalg_precision survivors are almost all in Wilkinson-shift computations (lines 613-639, 1043-1067) and in `_lp_sign`. Those affect the CONVERGENCE RATE of the implicit-QR sweep, not the answer, and the tests correctly assert the answer. Not a test gap; do not report.
- Likewise the spatial.cyr survivors: `_qt_bounds_cx/cy` (:389-390), the kd-tree split-plane pruning (:262-279) and the median-of-three pivot (:94-100) are all order/balance-only. `_qt_query_rec` re-tests every point against the query box, so a wrong cell centre degrades the tree without changing the result. `mid_x`/`mid_y` in the delaunay super-triangle (collision_mesh.cyr:179-180) is the same story - d_max = 10*max(dx,dy) keeps the super-triangle enclosing regardless. Benign.
- `opt_gradient_descent` (tests/hisab.tcyr:821-851) is the best-tested function in the tree: the full 10-step trajectory is pinned in closed form to EPSILON_F64, the converged point is pinned, `out_fmin == f


## Disposition

Too large to absorb in one release: **42 findings, 4 of them critical**. 2.8.1 is the audit itself.
The work is scheduled across 2.8.x in `../development/roadmap.md`, ordered by severity, with the
four criticals first.

Per the standing rule established in 2.7.0 and re-confirmed by the disposition sweep that caught two
never-scheduled findings: **the finding list is the unit of disposition, not this summary.** Every
row in the table above gets an explicit outcome before 2.8.x is called closed.
